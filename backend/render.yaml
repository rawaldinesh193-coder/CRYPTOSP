services:
  - type: web
    name: cryptosp-api
    env: node
    region: ohio
    plan: free
    buildCommand: npm install && npx prisma generate && npx prisma db push && npm run build
    startCommand: node dist/index.js
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        value: postgresql://neondb_owner:npg_CZO6dgtDT3bX@ep-odd-dust-axohavgb.c-4.us-east-2.aws.neon.tech/neondb?sslmode=require
      - key: JWT_SECRET
        generateValue: true
      - key: PHOENIX_COIN_PRICE
        value: "10.00"
      - key: NODE_ENV
        value: "production"
